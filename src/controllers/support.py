from typing import Union, Optional, List, Tuple


def error(
    location: str,
    comment: Optional[str] = None
):
    print(location, comment)


def reward(
    location: str,
    comment: Optional[str] = None
):
    print(location, comment)
